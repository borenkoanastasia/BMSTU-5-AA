#ifndef INPUT_HPP
#define INPUT_HPP

#include "structs.hpp"
#include <cstdlib>
#include <iostream>

void input_arr(vector_t &arr);
void generate_sort_arr(vector_t &arr);
void generate_reverse_arr(vector_t &arr);
void generate_random_arr(vector_t &arr);
void menu();
#endif